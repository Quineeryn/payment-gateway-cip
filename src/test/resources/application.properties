# Test Properties
spring.application.name=payment-gateway-test

# Database for Local Testing
spring.datasource.url=jdbc:postgresql://localhost:5432/payment_db
spring.datasource.username=postgres
spring.datasource.password=PasswordBaru123!

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Kafka (Disabled for tests to avoid needing a local Kafka)
app.kafka.enabled=false
spring.kafka.bootstrap-servers=localhost:9092
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# External Services (Mocking)
corebank.service.url=http://localhost:8080
biller.service.url=http://localhost:8080

# JWT for Test
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000
